<Window
    x:Class="QuickMsiBuilder.UI.BuildResultWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Quick MSI Builder"
    Width="460"
    SizeToContent="Height"
    Icon="Resources/dev.ico"
    PreviewKeyDown="OnWindowKeyDown"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/QuickMsiBuilder.UI;component/Themes/Styles/LightTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="txtHeadline"
            Grid.Row="0"
            Margin="16,16,16,0"
            FontSize="15"
            FontWeight="SemiBold"
            TextWrapping="Wrap" />

        <TextBlock
            x:Name="txtSummary"
            Grid.Row="1"
            Margin="16,8,16,0"
            Opacity="0.8"
            TextWrapping="Wrap" />

        <!--  Read-only but selectable, so the path can be copied  -->
        <TextBox
            x:Name="txtPath"
            Grid.Row="2"
            Margin="16,10,16,0"
            Padding="4,3"
            IsReadOnly="True"
            TextWrapping="Wrap" />

        <Grid Grid.Row="3" Margin="16,16,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                x:Name="btnLog"
                Grid.Column="0"
                Width="90"
                Height="28"
                Click="OnOpenLog"
                Content="Open log" />
            <Button
                x:Name="btnFolder"
                Grid.Column="2"
                Width="110"
                Height="28"
                Margin="0,0,8,0"
                Click="OnOpenFolder"
                Content="Open folder" />
            <Button
                x:Name="btnOk"
                Grid.Column="3"
                Width="90"
                Height="28"
                Click="OnOk"
                Content="OK"
                IsDefault="True" />
        </Grid>
    </Grid>
</Window>
